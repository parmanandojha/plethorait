// Demo blogs removed. Start with an empty list; blogs added via the
// admin portal are stored in localStorage and can later be wired here
// if you decide to persist them on the backend.
export const blogs = [];

